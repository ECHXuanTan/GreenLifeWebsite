{
  "name": "greenlifewebsite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd be && npm install  && cd ../fe && npm install && npm run build",
    "start": "concurrently \"cd be && npm start\" \"cd fe && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-router-dom": "^6.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
